@0
MOV RIGHT, DOWN

@1
MOV UP, RIGHT
MOV UP, DOWN
MOV UP, LEFT

@2
MOV LEFT, DOWN

@3
INIT:MOV ACC, DOWN
     MOV ACC, DOWN

START: MOV UP, ACC
       JEZ ZERO

ELSE: MOV 0, ACC
      JMP END

ZERO: MOV 1, ACC

END: MOV ACC, DOWN
     MOV ACC, DOWN
     MOV ACC, DOWN
     JMP START

@4
INIT:MOV ACC, DOWN


START: MOV UP, ACC
       JEZ ZERO

ELSE: MOV 0, ACC
      JMP END

ZERO: MOV 1, ACC

END: MOV ACC, DOWN
     MOV ACC, DOWN
     MOV ACC, DOWN
     JMP START

@5



START: MOV UP, ACC
       JEZ ZERO

ELSE: MOV 0, ACC
      JMP END

ZERO: MOV 1, ACC

END: MOV ACC, DOWN
     MOV ACC, DOWN
     MOV ACC, DOWN
     JMP START

@6
## MEGA-COUNTER

@7
MOV UP, RIGHT

@8
ADD LEFT
ADD UP
MOV ACC, RIGHT
MOV 0, ACC

@9
START:
  MOV -3, ACC
  ADD LEFT
  ADD UP
  JNZ ELSE


PATTERN:
  MOV 1, DOWN
  JMP START

ELSE:
  MOV 0, DOWN

@10
